# Jira API Token (Personal Access Token)
# Set this in Azure App Service → Configuration → Application settings
# When set here, the Admin UI token input is disabled (env var takes precedence)
JIRA_API_TOKEN=

PORT=3000
